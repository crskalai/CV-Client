import { TranslateHashPipe } from './translate-hash.pipe';

describe('TranslateHashPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateHashPipe();
    expect(pipe).toBeTruthy();
  });
});
